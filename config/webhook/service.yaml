apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: cache-system
spec:
  ports:
    - port: 443
      targetPort: 9443
      protocol: TCP
      name: webhook
  selector:
    control-plane: controller-manager
